{
  "buildCommand": "pnpm run typecheck && BASE_PATH=/ pnpm --filter @workspace/umroh-app build && BASE_PATH=/mockup-sandbox pnpm --filter @workspace/mockup-sandbox build && pnpm --filter @workspace/api-server build",
  "outputDirectory": "artifacts/umroh-app/dist/public",
  "rewrites": [
    {
      "source": "/mockup-sandbox/(.*)",
      "destination": "/mockup-sandbox/$1"
    },
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
